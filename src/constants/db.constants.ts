export enum COLLECTIONS {
  User = 'users'
}

export enum ENTITY_STATUS {
  AVAILABLE = 'AVAILABLE',
  UNAVAILABLE = 'UNAVAILABLE',
  HIDDEN = 'HIDDEN',
  DELETED = 'DELETED'
}

export enum ENTITY_SORT {
  ASC = 'asc',
  DESC = 'desc'
}
